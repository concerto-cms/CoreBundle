language: php

php:
  - 5.5
  - 5.4

before_script:
  - wget --no-check-certificate https://raw.githubusercontent.com/chluehr/roundsman/master/travis-phing.sh
  - chmod a+x travis-phing.sh

script: ./travis-phing.sh

#script:
#  - phpcs --report=summary --standard=PSR2 .
